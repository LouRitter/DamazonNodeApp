var mysql      = require('mysql');
var prompt     = require('prompt');

function options(){
	console.log("Please Select an option: (1)Show sales by department. (2)Create new department.")
	prompt.get(['options'],function(err,result){
		var options = result.option;
		if (option== 1){
			
		}
	});
};